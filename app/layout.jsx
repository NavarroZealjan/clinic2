import './globals.css';
import { AuthProvider } from '../contexts/auth'; // relative import

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
